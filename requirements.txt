aiogram>=3.31,<4
# yt-dlp is also upgraded to the latest release on every container start (see entrypoint.sh).
yt-dlp[default]>=2026.8.19
# Fetches YouTube PO tokens from the pot-provider service (needed for 720p+ from server IPs).
bgutil-ytdlp-pot-provider
